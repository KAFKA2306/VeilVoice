<Window x:Class="VeilVoice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VeilVoice" Height="500" Width="400"
        Background="#121212" Foreground="White"
        WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="MetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style x:Key="MetricValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00f2ff"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" BorderBrush="#333" BorderThickness="1" Background="#1A1A1A">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="SpaceBetween">
                <TextBlock Text="VEILVOICE" FontSize="24" FontWeight="Black" Foreground="#00f2ff"/>
                <Button Content="✕" Background="Transparent" BorderBrush="Transparent" Foreground="#555" Click="Close_Click" Cursor="Hand"/>
            </StackPanel>

            <!-- Status & Meters -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Input Meter" Style="{StaticResource MetricLabel}"/>
                <ProgressBar x:Name="InputMeter" Height="10" Background="#222" Foreground="#00f2ff" BorderThickness="0" Value="0" Margin="0,5,0,15"/>
                
                <TextBlock Text="Processed Output" Style="{StaticResource MetricLabel}"/>
                <ProgressBar x:Name="OutputMeter" Height="10" Background="#222" Foreground="#ff0055" BorderThickness="0" Value="0" Margin="0,5,0,15"/>
                
                <Grid Margin="0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="LATENCY" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="LatencyText" Text="-- ms" Style="{StaticResource MetricValue}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="STATUS" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="StatusText" Text="READY" Style="{StaticResource MetricValue}" Foreground="#888"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Device Info -->
            <StackPanel Grid.Row="2" Margin="0,20">
                <TextBlock Text="CURRENT MIC" Style="{StaticResource MetricLabel}"/>
                <TextBlock x:Name="MicName" Text="Default Microphone" FontSize="12" Foreground="#CCC"/>
                
                <TextBlock Text="VIRTUAL OUTPUT" Style="{StaticResource MetricLabel}"/>
                <TextBlock x:Name="VirtualMicName" Text="VeilVoiceOut" FontSize="12" Foreground="#CCC"/>
            </StackPanel>

            <!-- Action Button -->
            <Button Grid.Row="3" x:Name="StartButton" Content="START ENGINE" 
                    Height="50" FontSize="16" FontWeight="Bold"
                    Background="#00f2ff" Foreground="#121212"
                    BorderThickness="0" Cursor="Hand" Click="StartButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Border>
</Window>
